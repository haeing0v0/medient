<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="org.medient.mapper.MedicineMapper">

    <select id="findByUserId" resultType="org.medient.dto.medicine.MedicineResponseDTO">
        SELECT
            id,
            user_id AS userId,
            item_seq AS itemSeq,
            item_name AS itemName,
            DATE_FORMAT(start_date, '%Y-%m-%d') AS startDate,
            DATE_FORMAT(end_date, '%Y-%m-%d') AS endDate,
            daily_count AS dailyCount,
            dose_time AS doseTime,
            status,
            memo,
            DATE_FORMAT(created_at, '%Y-%m-%d %H:%i:%s') AS createdAt
        FROM user_medicine
        WHERE user_id = #{userId}
        ORDER BY id DESC
    </select>

    <select id="findTodayByUserId" resultType="org.medient.dto.medicine.MedicineResponseDTO">
	    SELECT
	        um.id,
	        um.user_id AS userId,
	        um.item_seq AS itemSeq,
	        um.item_name AS itemName,
	        DATE_FORMAT(um.start_date, '%Y-%m-%d') AS startDate,
	        DATE_FORMAT(um.end_date, '%Y-%m-%d') AS endDate,
	        um.daily_count AS dailyCount,
	        um.dose_time AS doseTime,
	        CASE
	            WHEN ml.medicine_id IS NOT NULL THEN '복용완료'
	            ELSE '복용전'
	        END AS status,
	        um.memo,
	        DATE_FORMAT(um.created_at, '%Y-%m-%d %H:%i:%s') AS createdAt
	    FROM user_medicine um
	    LEFT JOIN (
	        SELECT DISTINCT user_id, medicine_id
	        FROM medicine_log
	        WHERE taken_date = CURDATE()
	          AND status = '복용완료'
	    ) ml
	      ON um.id = ml.medicine_id
	     AND um.user_id = ml.user_id
	    WHERE um.user_id = #{userId}
	      AND CURDATE() BETWEEN um.start_date AND um.end_date
	      AND um.status != '중단'
	    ORDER BY um.id DESC
	</select>

    <insert id="insertMedicine">
        INSERT INTO user_medicine (
            user_id,
            item_seq,
            item_name,
            start_date,
            end_date,
            daily_count,
            dose_time,
            status,
            memo
        )
        VALUES (
            #{userId},
            #{itemSeq},
            #{itemName},
            #{startDate},
            #{endDate},
            #{dailyCount},
            #{doseTime},
            #{status},
            #{memo}
        )
    </insert>

    <delete id="deleteMedicine">
        DELETE FROM user_medicine
        WHERE id = #{id}
          AND user_id = #{userId}
    </delete>
    
    <select id="findById" resultType="org.medient.dto.medicine.MedicineResponseDTO">
    SELECT
        id,
        user_id AS userId,
        item_seq AS itemSeq,
        item_name AS itemName,
        DATE_FORMAT(start_date, '%Y-%m-%d') AS startDate,
        DATE_FORMAT(end_date, '%Y-%m-%d') AS endDate,
        daily_count AS dailyCount,
        dose_time AS doseTime,
        status,
        memo,
        DATE_FORMAT(created_at, '%Y-%m-%d %H:%i:%s') AS createdAt
	    FROM user_medicine
	    WHERE id = #{id}
	      AND user_id = #{userId}
	</select>
	
	<update id="updateMedicine">
	    UPDATE user_medicine
	    SET
	        item_seq = #{dto.itemSeq},
	        item_name = #{dto.itemName},
	        start_date = #{dto.startDate},
	        end_date = #{dto.endDate},
	        daily_count = #{dto.dailyCount},
	        dose_time = #{dto.doseTime},
	        status = #{dto.status},
	        memo = #{dto.memo}
	    WHERE id = #{id}
	      AND user_id = #{dto.userId}
	</update>
	
	<insert id="insertMedicineLog">
	    INSERT INTO medicine_log (
	        user_id,
	        medicine_id,
	        taken_date,
	        status
	    )
	    VALUES (
	        #{userId},
	        #{id},
	        CURDATE(),
	        '복용완료'
	    )
	</insert>
	
	<select id="findActiveMedicinesForDur"
	        resultType="org.medient.dto.medicine.MedicineResponseDTO">
	    SELECT
	        id,
	        user_id AS userId,
	        item_seq AS itemSeq,
	        item_name AS itemName,
	        DATE_FORMAT(start_date, '%Y-%m-%d') AS startDate,
	        DATE_FORMAT(end_date, '%Y-%m-%d') AS endDate,
	        daily_count AS dailyCount,
	        dose_time AS doseTime,
	        status,
	        memo,
	        DATE_FORMAT(created_at, '%Y-%m-%d %H:%i:%s') AS createdAt
	    FROM user_medicine
	    WHERE user_id = #{userId}
	      AND status != '중단'
	      AND CURDATE() BETWEEN start_date AND end_date
	    ORDER BY id DESC
	</select>
</mapper>